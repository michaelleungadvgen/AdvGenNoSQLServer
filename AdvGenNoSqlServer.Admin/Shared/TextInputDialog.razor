// Copyright (c) 2026 AdvanGeneration Pty. Ltd.
// Licensed under the MIT License.
// See LICENSE.txt for license information.

<MudDialog>
    <DialogContent>
        <MudText Class="mb-4">@Message</MudText>
        <MudTextField T="string" 
                      @bind-Value="_inputValue"
                      Label="@Label"
                      Variant="Variant.Outlined"
                      Immediate="true"
                      Required="true" />
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="Submit" Disabled="string.IsNullOrWhiteSpace(_inputValue)">OK</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private MudDialogInstance MudDialog { get; set; } = default!;

    [Parameter]
    public string Title { get; set; } = "Input";

    [Parameter]
    public string Message { get; set; } = "Enter a value:";

    [Parameter]
    public string Label { get; set; } = "Value";

    private string _inputValue = "";

    private void Submit() => MudDialog.Close(DialogResult.Ok(_inputValue));
    private void Cancel() => MudDialog.Cancel();
}
